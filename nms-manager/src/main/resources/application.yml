server:
  port: 8092
  shutdown: graceful
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: 123456
    listener:
      direct:
        retry:
          enabled: true
          max-attempts: 5
      simple:
        retry:
          enabled: true
          max-attempts: 5